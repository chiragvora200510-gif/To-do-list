import { KanbanSidebar } from "@/components/kanban/sidebar"
import { KanbanBoard } from "@/components/kanban/board"
import { Star, Share2, MoreHorizontal } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Layout } from "lucide-react"

export default function Home() {
  return (
    <div className="flex h-screen overflow-hidden bg-background">
      <KanbanSidebar />

      <main className="flex-1 flex flex-col overflow-hidden">
        {/* Header */}
        <header className="flex h-12 items-center justify-between border-b px-4 bg-background/50 backdrop-blur-sm">
          <div className="flex items-center gap-2">
            <Layout className="h-4 w-4 text-muted-foreground/70" />
            <span className="text-[13px] font-medium text-foreground/80">Tasks Board</span>
          </div>

          <div className="flex items-center gap-1">
            <Button variant="ghost" size="sm" className="text-muted-foreground text-xs gap-1.5">
              Share
            </Button>
            <Button variant="ghost" size="icon" className="h-8 w-8 text-muted-foreground">
              <Star className="h-4 w-4" />
            </Button>
            <Button variant="ghost" size="icon" className="h-8 w-8 text-muted-foreground">
              <Share2 className="h-4 w-4" />
            </Button>
            <Button variant="ghost" size="icon" className="h-8 w-8 text-muted-foreground">
              <MoreHorizontal className="h-4 w-4" />
            </Button>
          </div>
        </header>

        {/* Board Area */}
        <div className="flex-1 overflow-hidden flex flex-col">
          <div className="px-12 pt-16 pb-8">
            <h1 className="text-4xl font-bold tracking-tight text-foreground flex items-center gap-3">
              <span className="text-3xl">🎯</span> Tasks Board
            </h1>
            <p className="mt-3 text-muted-foreground text-base max-w-2xl">
              A minimalist workspace for tracking team projects, priorities, and daily progress.
            </p>
          </div>

          <div className="flex-1 relative">
            <KanbanBoard />
          </div>
        </div>
      </main>
    </div>
  )
}
