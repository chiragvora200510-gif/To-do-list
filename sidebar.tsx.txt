import type React from "react"
import { Search, Clock, Settings, Plus, ChevronDown, Layout, Target, Users, FileText } from "lucide-react"
import { Button } from "@/components/ui/button"

export function KanbanSidebar() {
  return (
    <aside className="flex h-screen w-64 flex-col border-r bg-muted/30">
      {/* User Workspace */}
      <div className="flex h-14 items-center gap-2 px-4 hover:bg-muted/50 cursor-pointer">
        <div className="h-6 w-6 rounded bg-primary flex items-center justify-center text-[10px] font-bold text-white">
          TC
        </div>
        <span className="text-sm font-semibold truncate">Tech Team Workspace</span>
        <ChevronDown className="h-4 w-4 text-muted-foreground ml-auto" />
      </div>

      <div className="flex flex-col gap-0.5 px-2 py-4">
        <SidebarItem icon={<Search className="h-4 w-4" />} label="Search" shortcut="⌘K" />
        <SidebarItem icon={<Clock className="h-4 w-4" />} label="Updates" />
        <SidebarItem icon={<Settings className="h-4 w-4" />} label="Settings" />
      </div>

      <div className="flex-1 px-2 py-4 overflow-y-auto">
        <p className="px-3 py-1 text-[11px] font-bold text-muted-foreground uppercase tracking-wider">Workspace</p>
        <div className="flex flex-col gap-0.5 mt-2">
          <SidebarItem icon={<Layout className="h-4 w-4" />} label="Tasks Board" active />
          <SidebarItem icon={<Target className="h-4 w-4" />} label="Goals" />
          <SidebarItem icon={<Users className="h-4 w-4" />} label="Team Directory" />
          <SidebarItem icon={<FileText className="h-4 w-4" />} label="Documentation" />
        </div>
      </div>

      <div className="p-4 border-t">
        <Button
          variant="ghost"
          className="w-full justify-start gap-2 h-9 text-muted-foreground font-normal hover:bg-muted"
        >
          <Plus className="h-4 w-4" />
          New Page
        </Button>
      </div>
    </aside>
  )
}

function SidebarItem({
  icon,
  label,
  shortcut,
  active,
}: {
  icon: React.ReactNode
  label: string
  shortcut?: string
  active?: boolean
}) {
  return (
    <button
      className={`flex items-center gap-2.5 px-3 py-1.5 w-full rounded-md transition-colors group ${
        active ? "bg-muted text-foreground" : "text-muted-foreground hover:bg-muted/50 hover:text-foreground"
      }`}
    >
      <span className="text-muted-foreground group-hover:text-foreground">{icon}</span>
      <span className="text-sm font-medium flex-1 text-left">{label}</span>
      {shortcut && <span className="text-[10px] text-muted-foreground/50">{shortcut}</span>}
    </button>
  )
}
